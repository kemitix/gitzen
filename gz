#!/usr/bin/env python

import sys

from gitzen import status

print("`git zen` - prototype")

for i, arg in enumerate(sys.argv):
    if i != 1:
        continue
    match arg:
        case "status":
            status.status()
